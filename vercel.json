{
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter luneo-frontend run build",
  "outputDirectory": "apps/frontend/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
