{
  "name": "@luneo/billing-plans",
  "version": "1.0.0",
  "description": "Billing plans and quota definitions for Luneo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "echo 'No source files to build - package already built'",
    "dev": "echo 'No source files to build - package already built'",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "echo 'No source files to lint'"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "eslint": "^8.0.0"
  }
}

